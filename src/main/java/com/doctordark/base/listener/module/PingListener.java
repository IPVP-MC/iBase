package com.doctordark.base.listener.module;

import org.bukkit.event.Listener;

/**
 * Listens to the server ping and updates accordingly.
 */
public class PingListener implements Listener {

    // Player count message

    // message of the day

    // online / max player count

    // server favicon
}
