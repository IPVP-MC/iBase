package com.doctordark.base.cmd.old;

public class SlowchatCommand {/**extends EssentialsCommand {

 private final Essentials plugin;

 public SlowchatCommand(Essentials plugin) {
 super("slowchat");
 this.plugin = plugin;
 }

 @Override public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
 //boolean slowed = plugin.getServerSettings().isChatSlowed();
 //plugin.getServerSettings().setChatSlowed(!slowed);
 //Bukkit.broadcastMessage(ChatColor.BLUE + "Chat is " + (!slowed ? "now" : "no longer") + " in slow mode!");
 return true;
 }

 @Override public List<String> onTabComplete(CommandSender sender, Command cmd, String label, String[] args) {
 return Collections.emptyList();
 }
 */
}
