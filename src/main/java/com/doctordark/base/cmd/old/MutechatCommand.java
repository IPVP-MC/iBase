package com.doctordark.base.cmd.old;

public class MutechatCommand {/** extends EssentialsCommand {

 private final Essentials plugin;

 public MutechatCommand(Essentials plugin) {
 super("mutechat");
 this.plugin = plugin;
 }

 @Override public boolean onCommand(CommandSender sender, Command cmd, String label, String[] args) {
 //boolean muted = plugin.getServerSettings().isChatMuted();
 //plugin.getServerSettings().setChatMuted(!muted);
 //Bukkit.broadcastMessage(ChatColor.BLUE + "Chat is " + (!muted ? "now" : "no longer") + " restricted!");
 return true;
 }

 @Override public List<String> onTabComplete(CommandSender sender, Command cmd, String label, String[] args) {
 return Collections.emptyList();
 }
 */
}
